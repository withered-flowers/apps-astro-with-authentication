---
// Component imports and setup JavaScript go here!
import BaseLayout from "../layouts/BaseLayout.astro";
import RegisterForm from "../components/RegisterForm.svelte";

const title = "Register Page";
---
<script>
  // A simple navigation guard based on localStorage items
	if(localStorage.getItem("authentication_token")) {
		location.replace("/");
	}
</script>

<BaseLayout title={title}>
	<div slot="main" class="min-w-full min-h-screen flex flex-col justify-center items-center" >
		<!-- We will use the javascript here, need hydration -->
		<!-- <h1>Login (Fake)</h1>
		<LoginForm client:visible /> -->
		<h1 class="text-3xl mb-4">Register</h1>
		<!-- We will use the javascript here, need hydration -->
		<!-- <LoginFormReal client:visible /> -->
    <RegisterForm client:visible />
		<p class="mt-4">or... <a href="/login" class="underline text-blue-400 hover:text-blue-600">back to login</a></p>
	</div>
</BaseLayout>